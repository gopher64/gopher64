use crate::device;

pub fn read(
    _device: &mut device::Device,
    _channel: usize,
    _address: u16,
    _data: usize,
    _size: usize,
) {
}

pub fn write(
    _device: &mut device::Device,
    _channel: usize,
    _address: u16,
    _data: usize,
    _size: usize,
) {
}
